<% layout("/layouts/boilerplate") -%>
<style>
    #filters {
        overflow: hidden;
        white-space: nowrap;
        position: relative;
        border-bottom: 1px solid #eee;
        padding: 0.5rem 0;
    }

    .filters-track {
        display: inline-flex;
        animation: scrollLeft 25s linear infinite;
    }

    .filters-track:hover {
        animation-play-state: paused; /* pause on hover */
    }

    .filter {
        text-align: center;
        margin: 0 1rem;
        opacity: 0.8;
    }

    .filter:hover {
        opacity: 1;
        transform: scale(1.05);
        transition: all 0.3s ease;
        cursor: pointer;
        transform: translateY(-7px);
    }

    .filter i {
        font-size: 1.2rem;
    }

    .filter p {
        font-size: 0.8rem;
    }

    /* Scrolling Animation */
    @keyframes scrollLeft {
        0% {
            transform: translateX(0);
        }
        100% {
            transform: translateX(-50%);
        }
    }
</style>


<div id="filters">
  <div class="filters-track">
    <div class="filter"><i class="fa-solid fa-fire"></i><p>Trending</p></div>
    <div class="filter"><i class="fa-solid fa-bed"></i><p>Rooms</p></div>
    <div class="filter"><i class="fa-solid fa-city"></i><p>Iconic Cities</p></div>
    <div class="filter"><i class="fa-solid fa-umbrella-beach"></i><p>Beach</p></div>
    <div class="filter"><i class="fa-solid fa-water"></i><p>Lake Front</p></div>
    <div class="filter"><i class="fa-solid fa-mountain-sun"></i><p>Mountains</p></div>
    <div class="filter"><i class="fa-solid fa-campground"></i><p>Camping</p></div>
    <div class="filter"><i class="fa-solid fa-cow"></i><p>Farms</p></div>
    <div class="filter"><i class="fa-solid fa-snowflake"></i><p>Snow</p></div>
    <div class="filter"><i class="fa-solid fa-street-view"></i><p>Street View</p></div>
    <div class="filter"><i class="fa-solid fa-vihara"></i><p>Village</p></div>
    <div class="filter"><i class="fa-solid fa-igloo"></i><p>Igloo</p></div>

    <!-- Duplicate filters once for seamless looping -->
    <div class="filter"><i class="fa-solid fa-fire"></i><p>Trending</p></div>
    <div class="filter"><i class="fa-solid fa-bed"></i><p>Rooms</p></div>
    <div class="filter"><i class="fa-solid fa-city"></i><p>Iconic Cities</p></div>
    <div class="filter"><i class="fa-solid fa-umbrella-beach"></i><p>Beach</p></div>
    <div class="filter"><i class="fa-solid fa-water"></i><p>Lake Front</p></div>
    <div class="filter"><i class="fa-solid fa-mountain-sun"></i><p>Mountains</p></div>
    <div class="filter"><i class="fa-solid fa-campground"></i><p>Camping</p></div>
    <div class="filter"><i class="fa-solid fa-cow"></i><p>Farms</p></div>
    <div class="filter"><i class="fa-solid fa-snowflake"></i><p>Snow</p></div>
    <div class="filter"><i class="fa-solid fa-street-view"></i><p>Street View</p></div>
    <div class="filter"><i class="fa-solid fa-vihara"></i><p>Village</p></div>
    <div class="filter"><i class="fa-solid fa-igloo"></i><p>Igloo</p></div>
  </div>
</div>

<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1">
    <% for(let listing of allListings){%>
        <a href="/listings/<%=listing._id%>" class="listing-link">
            <div class="card col listing-card" >
                <img src="<%= listing.image.url %>" class="card-img-top" 
                alt="<%= listing.title %>" 
                style="height: 20rem;">
                    <!-- To add effect on card when we hover over it -->
                <div class="card-img-overlay"></div>
                    <div class="card-body"> 
                        <p class="card-text">
                            <b><%= listing.title%></b><br/>
                            <%= listing.price ? listing.price.toLocaleString("en-IN") : "N/A" %> /night
                            <!-- <i class="tax-info">&nbsp;&nbsp; +18% GST</i> -->
                        </p>
                    </div>
            </div>
        </a>
    <%}%>
</div>